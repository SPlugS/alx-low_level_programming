#include "main.h"
/**
 *_memcpy - duplicates the intended  memory area
 *@dest: assigned memory space
 *@src: assigned memory space
 *@n: specified  number of bytes
 *
 *Return: duplicated memory with changes to the n byte
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	int c = 0;
	int f = n;

	for (; c < i ; c++)
	{
		dest[c] = src[c];
		n--
	}
	return (dest);
}

