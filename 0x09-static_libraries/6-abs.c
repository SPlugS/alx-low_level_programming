#include "main.h"
 
/**
 * _abs - evaluates the absolute value of an integer.
 * @n: integer  to evaluated
 * Return: the difinitive value of integer
 */
int _abs(int n)
{
        	if (n >= 0)
        	{
                    	return (n);
        	}
        	return (-n);
}

