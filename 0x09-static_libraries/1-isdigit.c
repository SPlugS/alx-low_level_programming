#include "main.h"
                    	
/**
 * _isdigit - searches  for  values
 * @c: values  that should be checked
 * Return: 1 if c is a digit, if not 0
 */
int _isdigit(int c)
{
        	return (c >= '0' && c <= '9');
}

