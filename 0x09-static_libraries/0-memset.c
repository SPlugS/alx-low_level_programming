#include "main.h"

/**
 * _memset - places specific values to sting of char.
 * @s: initial  address of memory to be allocated.
 * @b: the started value
 * @n: amunt of bytes to be replaced
 * Return: Pointer
 */
char *_memset(char *s, char b, unsigned int n)
{

   	int c = 0;

   	for (; n > 0; a++)
   	{
          	s[c] = b;
          	n--;
   	}
   	return (s);

}

